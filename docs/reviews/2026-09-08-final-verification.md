# AlphaPing 最终验收（2026-09-08）

已移除默认 Logo 外壳左上角的独立高光弧线，保留玻璃边缘、渐变、回波和信号节点；导航栏默认资源与 favicon 内容一致。最终效果见 [Logo 与实际页面](2026-09-08-glass-logo.md)。

本次在 Node 26.5.0、pnpm 12.3.4、macOS 上完成此前累积改动的本地验收，代码范围为 `f053795..ce0acd0`。此记录补充此前各轮报告中的待验证项，不代表已部署或完成原生发布矩阵。

| 检查                               | 结果                                                                                                                                                                                         |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm verify`                      | 完整通过：格式、lint、TypeScript 6 检查、单元/集成测试、D1 迁移和 schema manifest、成本模型、Rust fmt/clippy/test、资源测量、生产构建、Web E2E、6 个 Worker production dry-run、差异空白检查 |
| `node scripts/test-ingest-e2e.mjs` | 通过，包括注册、加密 report/ACK、重复/重放/篡改、配置与命令下发、密钥轮换、权限撤销、容器目录和状态落库                                                                                      |
| Rust workspace tests               | 90 项通过                                                                                                                                                                                    |
| Agent 资源采样                     | 30.2 秒、31 次采样，最大 RSS 15.67 MiB，平均 CPU 0.033%；为本机离线、未开启容器监控的短时样本                                                                                                |
| 依赖与 CI                          | npm audit 无已知漏洞；cargo-deny advisories/licenses/sources 通过；actionlint 通过                                                                                                           |
| 密钥扫描                           | 新增文件零命中；差异中两条命中为同一固定合成 LIVE_TICKET_SECRET 的增删行，无新增真实密钥                                                                                                     |
| Logo 浏览器复核                    | Chromium 公开页/外观设置 × 桌面/手机 × 明暗主题，共 8 组通过，无页面错误、横向溢出或 axe WCAG A/AA 违规；使用本地合成数据                                                                    |
| SVG                                | 3284 字节，favicon 与默认 Logo 一致；16/24/32/40/64/112/256 px 图片解码通过，无模糊滤镜、动画、脚本或外链                                                                                    |

最初的 Rust 容量测试受本机系统磁盘剩余空间低于 5% 影响，触发了预期的磁盘压力淘汰行为。将 `TMPDIR` 指向空间充足的磁盘后重新执行完整验证通过，没有修改业务逻辑或跳过测试。此前连接资源问题阻塞的完整 Web/D1/ingest 流程，本轮也均已通过。

部署前须先应用 CONTROL_DB 的 `0024_dashboard_appearance.sql`。Agent 的本地 probe configuration 已迁移为加密格式；旧 Agent 不能直接读取，升级前须保留一致的身份、配置和 spool 备份，回滚使用支持该格式的版本。

尚未执行远程 CI、远端部署、六个平台的原生安装/重启/签名更新矩阵或跨浏览器/真机视觉测试。长期、高并发探测和大型容器环境的资源上限仍须在对应环境验证。
