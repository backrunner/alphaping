{
  "name": "@alphaping/worker-notifications",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "repository": "https://github.com/alkinum/alphaping",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env= --config wrangler.notifications.template.toml --outdir dist",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --config wrangler.notifications.template.toml --test-scheduled",
    "test": "vitest run",
    "types": "wrangler types src/env.d.ts --include-runtime=false --env-file .dev.vars.example --config wrangler.notifications.template.toml"
  },
  "dependencies": {
    "@alphaping/contracts": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "miniflare": "4.20260730.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
