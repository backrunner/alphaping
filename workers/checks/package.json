{
  "name": "@alphaping/worker-checks",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env= --config wrangler.checks.template.toml --outdir dist",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --config wrangler.checks.template.toml --test-scheduled",
    "test": "vitest run",
    "types": "wrangler types src/env.d.ts --include-runtime=false --env-file .dev.vars.example --config wrangler.checks.template.toml"
  },
  "dependencies": {
    "@alphaping/contracts": "workspace:*",
    "@alphaping/db": "workspace:*",
    "ipaddr.js": "2.4.0",
    "jsonpath-plus": "10.4.0",
    "re2js": "2.8.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "miniflare": "4.20260710.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
